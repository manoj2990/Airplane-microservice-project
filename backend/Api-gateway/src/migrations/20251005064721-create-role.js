'use strict';
/** @type {import('sequelize-cli').Migration} */
const { USER_ROLE } = require('../utils/common/enums');

module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.createTable('Roles', {
      id: {
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
        type: Sequelize.INTEGER
      },
      name: {
        type: Sequelize.ENUM,
        values: [USER_ROLE.CUSTOMER, USER_ROLE.ADMIN, USER_ROLE.FLIGHT_COMPANY],
        defaultValue: USER_ROLE.CUSTOMER,
        allowNull: false,
      },
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE
      }
    });
  },
  async down(queryInterface, Sequelize) {
    await queryInterface.dropTable('Roles');
  }
};